{{- if and .Values.dashboard.enabled .Values.service.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "cnsl.fullname" . }}-test-connection
  labels:
    {{- include "cnsl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: health-check
      image: curlimages/curl:8.10.1
      command:
        - sh
        - -c
        - |
          curl -sf "http://{{ include "cnsl.fullname" . }}:{{ .Values.service.port }}/api/health" \
            || (echo "CNSL dashboard health check failed" && exit 1)
{{- end }}