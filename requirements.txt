# ============================================================
# CNSL — Python Requirements
# ============================================================
# Core engine has ZERO external dependencies (pure stdlib).
# Install extras based on what features you need.
#
# Quick install (everything):
#   pip install -r requirements.txt
#
# Minimal install (core only):
#   pip install -e .
# ============================================================

# ── Optional: Web dashboard + REST API + Notifications ──────
aiohttp>=3.9.0

# ── Optional: SQLite async persistence ──────────────────────
aiosqlite>=0.19.0

# ── Optional: YAML config file support ──────────────────────
pyyaml>=6.0.0

# ── Development & Testing ───────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.23.0

# ── Optional: Dashboard JWT authentication ───────────────────────────────────
bcrypt>=4.0.0
PyJWT>=2.8.0

# ── Optional: MaxMind offline GeoIP ─────────────────────────────────────────
geoip2>=4.7.0

# ── Optional: Redis distributed blocklist ────────────────────────────────────
redis>=5.0.0

# ── Optional: AbuseIPDB threat intelligence ──────────────────────────────────
# (uses aiohttp already listed above)

# ── Phase 3: ML anomaly detection ────────────────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0

# ── Phase 3: Compliance PDF reports ──────────────────────────────────────────
reportlab>=4.0.0

# setuptools
setuptools